#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);

    int N;
    cin >> N;
    string s;
    cin >> S;

    for(int i=0;i<N;i++){
        int a=i
        int b=i+1;
        
    }

    return 0;
}