#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);

    int N;
    cin >> N;
    vector<string> A(N);
    for(int i=0;i<N;i++)
        cin >> A[i];

    vector<vector<vector<vector<int>>>> x; // x[x1][y1][x2][y2] 50x50x50x50
    

    return 0;
}