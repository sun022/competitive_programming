#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);

    int H, N;
    cin >> H >> N;
    vector<int> D(N);
    for(int i=0;i<N;i++)
        cin >> D[i];

    return 0;
}