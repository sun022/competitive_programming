#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);

    int N;
    cin >> N;

    for(int i=0;i<N;i++){
        int K; cin >> K;
        
        for(int k=0;k<K;k++){
            int x,y;
            cin >> x >> y;
        }
    }
    vector<int> A(N);
    for(int i=0;i<N;i++)
        cin >> A[i];

    return 0;
}